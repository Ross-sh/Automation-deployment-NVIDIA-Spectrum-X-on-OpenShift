---
- name: Configure Physical Rail Interface Attributes
  hosts: localhost
  connection: local
  gather_facts: false

  vars_files:
    - ../vars/ocp_connect.yml

  roles:
    - role: ../roles/fwctl_krn_module
