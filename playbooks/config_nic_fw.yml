---
- name: Configuring Nic Firmware
  hosts: localhost
  connection: local
  gather_facts: false

  vars_files:
    - ../vars/ocp_connect.yml

  roles:
    - role: ../roles/openshift_nvidia_nic_firmware
