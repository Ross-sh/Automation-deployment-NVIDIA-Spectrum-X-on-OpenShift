---
- name: Enable native RHCOS lldpad service via MachineConfig
  hosts: localhost
  connection: local
  gather_facts: false

  vars_files:
    - ../vars/ocp_connect.yml

  roles:
    - role: ../roles/lldp_machineconfig
