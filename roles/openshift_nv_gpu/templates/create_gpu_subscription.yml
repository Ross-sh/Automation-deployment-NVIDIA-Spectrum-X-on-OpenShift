---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: "{{ nv_gpu_sub_name }}"
  namespace: "{{ gpu_operator_namespace }}"
spec:
  channel: "{{ gpu_operator_channel }}"
  installPlanApproval: "{{ nv_gpu_install_plan }}"
  name: "{{ nv_gpu_sub_name }}"
  source: "{{ nv_gpu_src }}"
  sourceNamespace: "{{ nv_gpu_src_nms }}"
